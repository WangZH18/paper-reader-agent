# Paper Reader Agent

一个用于 Claude Code 的学术论文阅读助手 Agent，帮助用户理解、分析和总结学术论文。

## 功能特点

- 结构化分析学术论文（背景、方法、实验、结论）
- **HTML 双栏对照视图**：左栏嵌入 PDF 原文，右栏显示渲染后的分析内容，支持同屏对照阅读
- **可点击页码跳转**：分析内容中的红色 `[p.XX]` 标注可点击，左栏 PDF 自动跳转到对应页
- **内联 Markdown 渲染器**：零外部依赖（无需 CDN），内置轻量 `miniMd()` 渲染引擎，支持标题、列表、表格、代码块等
- **智能页码偏移**：自动处理期刊封面页导致的 PDF 物理页码与论文页码不一致问题
- 同时生成 `.md` 和 `.html` 两种格式的分析文件
- 自动复制源 PDF 到输出目录，HTML 文件可独立打开使用
- 维护论文索引（`_index.md`），便于管理多篇论文
- 中文输出，技术术语双语呈现

## 安装

将 `paper-reader.md` 复制到 Claude Code 的 agents 目录：

```bash
# Windows
copy paper-reader.md %USERPROFILE%\.claude\agents\

# macOS/Linux
cp paper-reader.md ~/.claude/agents/
```

## 使用方法

在 Claude Code 中，当你需要分析论文时，agent 会自动被调用。示例：

- "帮我分析这篇论文"
- "这篇论文的主要贡献是什么？"
- "解释一下第3节的方法"

## 输出文件说明

每次分析论文后，输出目录下会生成以下文件：

| 文件 | 说明 |
|------|------|
| `[主题].pdf` | 源 PDF 副本 |
| `论文解读_[主题].html` | HTML 双栏对照视图（推荐使用） |
| `论文解读_[主题].md` | Markdown 格式分析文档 |
| `_index.md` | 论文分析索引 |

## 配置

默认输出路径为 `./output/`，可在 `paper-reader.md` 中修改 `保存路径` 配置。

## 更新日志

### v2.0.0 (2026-02-22)
- **新增** HTML 双栏对照视图：左栏 PDF 原文 + 右栏分析内容，支持同屏对照阅读
- **新增** 可点击页码跳转：红色 `[p.XX]` 标注点击后 PDF 自动跳转对应页
- **新增** 内联 `miniMd()` Markdown 渲染引擎，零外部 CDN 依赖，国内网络无障碍使用
- **新增** 智能页码偏移（`PAGE_OFFSET`），自动适配期刊封面页导致的页码偏差
- **新增** 同时输出 `.md` + `.html` 两种格式
- **新增** 自动复制源 PDF 到输出目录，HTML 文件可独立分享使用
- **优化** 工作流程从 6 步扩展为 8 步，增加 PDF 复制和 .md 文件生成步骤

### v1.0.0 (2026-02-21)
- 初始版本：结构化论文分析、Markdown 输出、页码标注、索引管理

## 许可证

MIT License

